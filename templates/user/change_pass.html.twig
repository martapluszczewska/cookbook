{% include 'default/_form.html.twig' with {
    page_title: 'title.password_edit',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('user_edit_pass', {'id': user.id}),
    },
    back_to_list_path: 'user_index',
} %}